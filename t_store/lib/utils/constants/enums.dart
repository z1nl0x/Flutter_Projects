// Listas de ENUMS
// Eles não podem serem criados dentro de uma classe

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods { paypal, googlePlay, applePlay, visa, masterCard, creditCard, paystack, razorPay, paytm }